services:
  app:
    build: .
    container_name: my-app   # keep in sync with service.yml container_name
    restart: unless-stopped
    networks:
      - orch-net

networks:
  orch-net:
    external: true
    name: orch-net   # matches the pinned name in Orchestrator2-Secure
